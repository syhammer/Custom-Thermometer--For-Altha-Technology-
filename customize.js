function CustomizeObject(thermometer)
{
  thermometer.bar.size.inner.x = 300;
  thermometer.bar.size.inner.y = 6;
  thermometer.bar.size.outer.x = width;
  thermometer.bar.size.outer.y = height;
  thermometer.bar.borderRadius = 3;
}
